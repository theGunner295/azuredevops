---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent
  labels:
    app.kubernetes.io/name: azure-pipelines-agent
    app.kubernetes.io/instance: agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: azure-pipelines-agent
      app.kubernetes.io/instance: agent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: azure-pipelines-agent
        app.kubernetes.io/instance: agent
    spec:
      containers:
        - name: azure-pipelines-agent
          image: "awesomejack295/azuredevops-agent:latest"
          imagePullPolicy: Always
          env:
            - name: AZP_AGENT_NAME
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
            - name: AZP_URL
              value: "https://dev.azure.com/RedshiftEnt"
            - name: AZP_POOL
              value: "RS-03-KUB-01"
            - name: AZP_TOKEN
              valueFrom: 
                secretKeyRef:
                  key: vg4bpyxriwwh5kkecc6vjrmtim5jragefkpnqhqpa2quocwqfbha
                  name: RS-03-KUB-01